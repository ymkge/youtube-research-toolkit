# フロントエンド (Next.js) 実装ステータス & アーキテクチャ

本ドキュメントは、YouTube Research Toolkit のフロントエンド実装状況およびコンポーネント構造の最新ドキュメントです。

---

## 1. 完了済み機能とコンポーネント構成

### A. メインダッシュボード画面 (`src/app/page.tsx`)
* **ヘッダー＆ビュー切替タブ**:
  * 「📊 ダッシュボード」と「📈 成長率比較分析」のタブ切替ナビゲーション。
  * 現在追跡中のチャンネル数を可視化するバッジ表示。
* **チャンネル登録フォーム (`ChannelRegisterForm.tsx`)**:
  * チャンネルID（`UC...`）またはハンドル（`@...`）の入力による即時データ同期。
* **多角ソートコントロール**:
  * 「カスタム順 (ドラッグ順)」「登録者数順」「総再生数順」「動画数順」「平均再生数順」の各指標によるリアルタイム並び替え (#34)。
  * 降順選択時は、最も数値の高いチャンネルが **「左上 ➔ 中央 ➔ 右上 ➔ 2段目...」** の優先順位で配置。
  * **ピン留め優先制御**: ピン留めチャンネルは最上部エリアに維持。

### B. チャンネルカード (`ChannelCard.tsx`)
* **ランク別ダークグラデーション表現**:
  * 💎 **Diamond** (10万人〜) / 🥇 **Gold** (1万人〜) / 🥈 **Silver** (1,000人〜) / 🥉 **Bronze** (< 1,000人)
* **🔥 前日比急成長 注目フラグ (#36)**:
  * 前日比で登録者数が **+100名以上** 伸びているチャンネルに脈動ネオンバッジ（`🔥 前日比 +XXX名`）を自動表示。
* **インタラクティブ・操作機能**:
  * ドラッグ＆ドロップによる自由な並び替え・ピン留め切替。
  * インライン時系列トレンドグラフ展開 (`ChannelHistoryChart.tsx`)。
  * AIポジショニング分析モーダル起動。

### C. 成長率比較分析画面 (`GrowthComparisonView.tsx`)
* **累積成長率 (%) 比較 2連 Recharts グラフ**:
  * 上段: 登録者数の成長率 (%) / 下段: 総再生数の成長率 (%)。
* **🎨 多重データ視認性向上機能 (#28)**:
  * 20色ユニークカラー ＋ 実線/破線パターンの自動適用。
  * 凡例ホバー時の対象線ハイライト強調（太線化 ＋ 他線半透明化）。
* **📌 スクロール追従型 (Sticky) 左サイドバー**:
  * 画面下部閲覧中も常に追従するフィルターパネル。
* **⚡ 規模一括閾値フィルター (#35)**:
  * `💎 10万+` `🥇 1万+` `🥈 1千+` `📌 ピン留め` のワンタップ一括選択ボタン。
  * 「登録者数 [ `数値` ] 人以上」の自由数値入力フォーム。

### D. AIポジショニング分析モーダル (`AIAnalysisModal.tsx`)
* **RAG (ドメイン知識注入) 対応レポート表示**:
  * 強み、弱み、ヒットテーマ、BGMジャンルの特性を踏まえた差別化ポジショニングアドバイスの可視化。
* **🔄 「最新ノウハウで再分析」機能**:
  * モーダル内から `domain_knowledge.txt` の最新知見を反映した再分析（強制キャッシュ更新）の一発実行。

---

## 2. ディレクトリ構成

```text
frontend/src/app/
├── components/
│   ├── AIAnalysisModal.tsx             # AI分析結果モーダル
│   ├── AIAnalysisModal.module.css
│   ├── ChannelCard.tsx                 # 競合チャンネルカード
│   ├── ChannelCard.module.css
│   ├── ChannelHistoryChart.tsx         # カード内インライン時系列グラフ
│   ├── ChannelRegisterForm.tsx         # チャンネル登録フォーム
│   ├── GrowthComparisonView.tsx        # 成長率比較分析ビュー (Sticky左サイドバー & 一括フィルター)
│   └── GrowthComparisonView.module.css
├── utils/
│   └── api.ts                          # バックエンドAPI通信モジュール
├── page.tsx                            # メインアプリケーションエントリーポイント
└── page.module.css                     # メインページスタイル
```
